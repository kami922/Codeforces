# Import the sys module to read input efficientlyimport sys # Read the number of test casest = int(sys.stdin.readline().strip()) # Loop through each test casefor _ in range(t):	# Read the size of the permutation	n = int(sys.stdin.readline().strip())	# Read the permutation elements and store them in a list	a = list(map(int, sys.stdin.readline().strip().split()))	# Check if the first element of the permutation is 1	if a[0] == 1:		print("YES")  # If true, print "YES"	else:		print("NO")  # Otherwise, print "NO" # Time Complexity (TC): O(n) = O(10)# Space Complexity (SC): O(n) = O(10)